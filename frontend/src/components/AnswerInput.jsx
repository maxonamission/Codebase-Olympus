export default function AnswerInput({ question, onAnswer, disabled }) {
  return (
    <div className="answer-input">
      <p className="text-muted">Antwoordinvoer — wordt uitgewerkt.</p>
    </div>
  )
}
